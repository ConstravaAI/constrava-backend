import { promises as fs } from "node:fs";
import path from "node:path";
import { fileURLToPath, pathToFileURL } from "node:url";

const here = path.dirname(fileURLToPath(import.meta.url));
const responsiveSourcePath = path.join(here, "server-responsive.js");
const analyticsRuntimePath = path.join(here, ".server-responsive-analytics.js");

const wideCss = String.raw`
/* Wide app layout layer: reduce dead space and make the dashboard feel full-screen */
html,body{min-height:100%}.shell{width:calc(100% - 18px)!important;max-width:none!important;margin:10px auto!important}.topbar{padding:10px 14px!important}.workspace{margin-bottom:10px!important;gap:10px!important;align-items:center!important}.workspace h1{font-size:36px!important}.workspace p{margin:0 0 3px!important}.grid{gap:12px!important}.card{border-radius:14px!important}.in{padding:14px!important}.metrics{grid-template-columns:repeat(4,minmax(0,1fr))!important}.two{grid-template-columns:minmax(0,1fr) minmax(0,1fr)!important}.metricValue{font-size:29px!important}.item{padding:10px 0!important}.crmShell{grid-template-columns:210px minmax(0,1fr)!important;gap:12px!important;align-items:start!important}.crmSide{top:72px!important;border-radius:14px!important;padding:8px!important;height:calc(100vh - 82px)!important;max-height:calc(100vh - 82px)!important;overflow-y:auto!important;overflow-x:hidden!important;display:flex!important;flex-direction:column!important}.crmTab{padding:9px 10px!important;border-radius:10px!important;flex:0 0 auto!important}.crmListHead{margin-bottom:6px!important}.crmToolbar{padding:9px 0!important;margin:4px 0 0!important}.crmToolbar input{min-width:min(360px,100%)!important}.recordCard{gap:8px!important}.fieldLine{margin-top:3px!important}.empty,.crmEmpty{min-height:120px!important;padding:20px!important}.resource{gap:10px!important}.resourceIcon{width:38px!important;height:38px!important;border-radius:12px!important}pre{padding:12px!important}.modalHead,.modalBody,.modalFoot{padding:14px!important}
@media(min-width:1500px){.shell{width:calc(100% - 22px)!important}.metrics{grid-template-columns:repeat(4,minmax(0,1fr))!important}.crmShell{grid-template-columns:220px minmax(0,1fr)!important}.workspace h1{font-size:38px!important}.in{padding:15px!important}}
@media(max-width:1100px){.shell{width:calc(100% - 14px)!important;margin:8px auto!important}.crmShell{grid-template-columns:1fr!important}.crmSide{height:auto!important;max-height:none!important;overflow-x:auto!important;overflow-y:hidden!important;display:flex!important;flex-direction:row!important}.metrics{grid-template-columns:repeat(2,minmax(0,1fr))!important}.two{grid-template-columns:1fr!important}}
@media(max-width:760px){.shell{width:calc(100% - 10px)!important;margin:8px auto!important}.topbar{padding:9px!important}.workspace{margin-bottom:8px!important}.workspace h1{font-size:30px!important}.in{padding:12px!important}.card{border-radius:12px!important}.metrics{grid-template-columns:1fr!important}.crmSide{padding:7px!important;height:auto!important;max-height:none!important;overflow-x:auto!important;overflow-y:hidden!important}.crmToolbar{padding:8px 0!important}.item{padding:9px 0!important}}
`;

const analyticsCss = String.raw`
/* Analytics command center layer */
.analyticsShell{display:grid;gap:12px}.analyticsTop{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;background:white;border:1px solid var(--line);border-radius:16px;padding:14px;box-shadow:0 14px 34px rgba(6,26,51,.07)}.analyticsTop h2{margin:0;color:var(--blue);font-size:28px;letter-spacing:-.04em}.analyticsControls{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.analyticsControls select,.analyticsControls button{height:42px}.analyticsKpis{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:12px}.analyticsKpi{background:white;border:1px solid var(--line);border-radius:16px;padding:14px;box-shadow:0 14px 34px rgba(6,26,51,.07)}.analyticsKpi p{margin:0;color:var(--muted);font-size:13px;font-weight:800}.analyticsKpi b{display:block;color:var(--blue);font-size:28px;margin:5px 0}.analyticsDelta{font-size:12px;font-weight:950}.analyticsDelta.up{color:#0f8f50}.analyticsDelta.down{color:#a83b3b}.analyticsMain{display:grid;grid-template-columns:1.45fr .85fr;gap:12px}.analyticsCard{background:white;border:1px solid var(--line);border-radius:16px;box-shadow:0 14px 34px rgba(6,26,51,.07)}.analyticsCard .in{padding:15px!important}.analyticsCard h3{margin:0 0 6px;color:var(--blue)}.analyticsBars{height:260px;display:flex;align-items:end;gap:8px;border-top:1px solid var(--line);border-bottom:1px solid var(--line);padding:12px 0;margin-top:12px}.analyticsBarWrap{flex:1;display:grid;align-items:end;gap:6px;min-width:0}.analyticsBar{border-radius:10px 10px 4px 4px;background:linear-gradient(180deg,#24c875,#0b7a45);min-height:6px}.analyticsBarLabel{font-size:11px;color:var(--muted);text-align:center;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.analyticsInsight{padding:12px 0;border-top:1px solid var(--line)}.analyticsInsight:first-child{border-top:0}.analyticsInsight b{color:var(--blue)}.analyticsInsight p{margin:4px 0 0;color:var(--muted)}.analyticsBreakdowns{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.analyticsRow{display:grid;grid-template-columns:1fr auto;gap:10px;align-items:center;padding:9px 0;border-top:1px solid var(--line)}.analyticsRow:first-child{border-top:0}.analyticsTrack{grid-column:1 / -1;height:8px;border-radius:999px;background:#eaf2ff;overflow:hidden}.analyticsFill{height:100%;border-radius:999px;background:#061a33}.analyticsFunnel{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:10px}.funnelStep{background:#f7fbff;border:1px solid var(--line);border-radius:14px;padding:12px;position:relative}.funnelStep b{display:block;color:var(--blue);font-size:24px}.funnelStep span{color:var(--muted);font-size:13px;font-weight:800}.analyticsTables{display:grid;grid-template-columns:1fr 1fr;gap:12px}.analyticsMiniRecord{padding:10px 0;border-top:1px solid var(--line)}.analyticsMiniRecord:first-child{border-top:0}.analyticsMiniRecord b{color:var(--blue)}.analyticsMiniRecord p{margin:4px 0 0;color:var(--muted)}
@media(max-width:1200px){.analyticsKpis{grid-template-columns:repeat(3,minmax(0,1fr))}.analyticsMain,.analyticsTables{grid-template-columns:1fr}.analyticsBreakdowns{grid-template-columns:1fr 1fr}.analyticsFunnel{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(max-width:760px){.analyticsKpis,.analyticsBreakdowns,.analyticsFunnel{grid-template-columns:1fr}.analyticsTop{align-items:stretch}.analyticsControls{display:grid;grid-template-columns:1fr}.analyticsControls select,.analyticsControls button{width:100%}.analyticsBars{height:210px;gap:4px}.analyticsTop h2{font-size:24px}}
`;

const analyticsClientCode = String.raw`function analyticsRangeDays(){const v=S.analyticsRange||'30';return v==='all'?99999:Number(v||30)}
function analyticsRangeLabel(){const v=S.analyticsRange||'30';return v==='all'?'All time':'Last '+v+' days'}
function analyticsCutoff(){return Date.now()-analyticsRangeDays()*86400000}
function analyticsDateValue(row){return Date.parse(row.createdAt||row.updatedAt||0)||0}
function analyticsSourceFilter(){return S.analyticsSource||'all'}
function analyticsRecords(){const cutoff=analyticsCutoff(),source=analyticsSourceFilter();return (S.records||[]).filter(function(r){if(analyticsRangeDays()<99999&&analyticsDateValue(r)<cutoff)return false;if(source!=='all'&&!(r.sourceIds||[]).includes(source))return false;return true})}
function analyticsEvents(){const cutoff=analyticsCutoff();return (S.events||[]).filter(function(e){return analyticsRangeDays()>=99999||(Date.parse(e.createdAt||0)||0)>=cutoff})}
function analyticsPreviousRecords(){const days=analyticsRangeDays();if(days>=99999)return [];const now=Date.now(),start=now-days*86400000,prev=start-days*86400000,source=analyticsSourceFilter();return (S.records||[]).filter(function(r){const t=analyticsDateValue(r);if(t<prev||t>=start)return false;if(source!=='all'&&!(r.sourceIds||[]).includes(source))return false;return true})}
function analyticsPct(current,previous){if(!previous&&current)return '+100%';if(!previous&&!current)return '0%';const n=Math.round(((current-previous)/Math.max(previous,1))*100);return (n>0?'+':'')+n+'%'}
function analyticsDeltaClass(current,previous){return current>=previous?'up':'down'}
function analyticsMoneyValue(rows){return rows.filter(function(r){return r.type==='Deal'}).reduce(function(sum,r){return sum+Number((r.fields||{}).value||0)},0)}
function analyticsKpi(name,value,current,previous,note){return '<div class="analyticsKpi"><p>'+esc(name)+'</p><b>'+esc(value)+'</b><span class="analyticsDelta '+analyticsDeltaClass(current,previous)+'">'+analyticsPct(current,previous)+'</span><p>'+esc(note||'vs previous period')+'</p></div>'}
function analyticsCounts(rows,fn){return rows.reduce(function(map,row){const key=fn(row)||'Unknown';map[key]=(map[key]||0)+1;return map},{})}
function analyticsPriorityName(r){const s=Number(r.priorityScore||0);if(s>=95)return 'Highest';if(s>=75)return 'High';if(s>=50)return 'Normal';return 'Low'}
function analyticsTopEntries(map){return Object.entries(map).sort(function(a,b){return b[1]-a[1]})}
function analyticsRows(title,map){const entries=analyticsTopEntries(map);const max=Math.max(1,...entries.map(function(e){return e[1]}));return '<section class="analyticsCard"><div class="in"><h3>'+esc(title)+'</h3>'+(entries.length?entries.map(function(e){const pct=Math.round((e[1]/max)*100);return '<div class="analyticsRow"><b>'+esc(e[0])+'</b><span class="pill">'+e[1]+'</span><div class="analyticsTrack"><div class="analyticsFill" style="width:'+pct+'%"></div></div></div>'}).join(''):'<p class="muted">No data yet.</p>')+'</div></section>'}
function analyticsDayKey(date){return new Date(date).toISOString().slice(5,10)}
function analyticsChart(records,events){const days=Math.min(analyticsRangeDays(),30);let buckets=[];if(days>=99999){const all=[...records,...events].sort(function(a,b){return (Date.parse(a.createdAt||0)||0)-(Date.parse(b.createdAt||0)||0)});const recent=all.slice(-14);buckets=recent.map(function(x){return {label:analyticsDayKey(x.createdAt||new Date()),records:0,events:0}});if(!buckets.length)buckets=[{label:'Today',records:0,events:0}]}else{for(let i=days-1;i>=0;i--){const d=new Date(Date.now()-i*86400000);buckets.push({label:analyticsDayKey(d),records:0,events:0})}}const index={};buckets.forEach(function(b){index[b.label]=b});records.forEach(function(r){const k=analyticsDayKey(r.createdAt||new Date());if(index[k])index[k].records++});events.forEach(function(e){const k=analyticsDayKey(e.createdAt||new Date());if(index[k])index[k].events++});const max=Math.max(1,...buckets.map(function(b){return b.records+b.events}));return '<div class="analyticsBars">'+buckets.map(function(b){const h=Math.max(6,Math.round(((b.records+b.events)/max)*100));return '<div class="analyticsBarWrap" title="'+esc(b.label+': '+b.records+' records, '+b.events+' events')+'"><div class="analyticsBar" style="height:'+h+'%"></div><div class="analyticsBarLabel">'+esc(b.label)+'</div></div>'}).join('')+'</div>'}
function analyticsInsights(records,events){const types=analyticsTopEntries(analyticsCounts(records,function(r){return r.type}));const topType=types[0]||['No records',0];const highest=records.filter(function(r){return Number(r.priorityScore||0)>=95});const tasks=records.filter(function(r){return r.type==='Task'});const overdue=tasks.filter(function(t){return (t.fields||{}).dueDate&&t.fields.dueDate<new Date().toISOString().slice(0,10)});const dealValue=analyticsMoneyValue(records);return '<div class="analyticsInsight"><b>'+esc(topType[0])+' is the most common record type</b><p>'+topType[1]+' matching records in '+analyticsRangeLabel().toLowerCase()+'.</p></div><div class="analyticsInsight"><b>'+highest.length+' highest-priority records need attention</b><p>These are records scored 95 or higher.</p></div><div class="analyticsInsight"><b>'+money(dealValue)+' in tracked opportunity</b><p>Based on deal values inside the selected range.</p></div><div class="analyticsInsight"><b>'+overdue.length+' overdue tasks</b><p>Open follow-ups with due dates before today.</p></div><div class="analyticsInsight"><b>'+events.length+' activity events captured</b><p>Website, tracker, and in-app events in this period.</p></div>'}
function analyticsFunnel(records){const intake=records.filter(function(r){return r.type==='Intake'}).length;const contacts=records.filter(function(r){return r.type==='Person'}).length;const companies=records.filter(function(r){return r.type==='Company'}).length;const deals=records.filter(function(r){return r.type==='Deal'}).length;const tasks=records.filter(function(r){return r.type==='Task'}).length;const steps=[['Intake',intake],['Contacts',contacts],['Companies',companies],['Deals',deals],['Tasks',tasks]];return '<section class="analyticsCard"><div class="in"><h3>Business funnel</h3><p class="muted">How raw activity turns into useful CRM work.</p><div class="analyticsFunnel">'+steps.map(function(s){return '<div class="funnelStep"><span>'+esc(s[0])+'</span><b>'+s[1]+'</b></div>'}).join('')+'</div></div></section>'}
function analyticsSourceOptions(){return '<option value="all">All sources</option>'+(S.sources||[]).map(function(s){return '<option value="'+esc(s.id)+'" '+(analyticsSourceFilter()===s.id?'selected':'')+'>'+esc(s.name)+'</option>'}).join('')}
function analyticsControls(){const range=S.analyticsRange||'30';function sel(v){return range===v?'selected':''}return '<div class="analyticsControls"><select id="analyticsRange"><option value="7" '+sel('7')+'>Last 7 days</option><option value="30" '+sel('30')+'>Last 30 days</option><option value="90" '+sel('90')+'>Last 90 days</option><option value="all" '+sel('all')+'>All time</option></select><select id="analyticsSource">'+analyticsSourceOptions()+'</select><button class="secondary" id="analyticsRefresh">Refresh</button><button class="primary" id="analyticsReport">Generate report</button></div>'}
function analyticsContent(){S.analyticsRange ||= '30';S.analyticsSource ||= 'all';const records=analyticsRecords(),events=analyticsEvents(),prev=analyticsPreviousRecords();const leads=records.filter(function(r){return r.type==='Person'||r.type==='Intake'});const prevLeads=prev.filter(function(r){return r.type==='Person'||r.type==='Intake'});const deals=records.filter(function(r){return r.type==='Deal'});const tasks=records.filter(function(r){return r.type==='Task'});const highest=records.filter(function(r){return Number(r.priorityScore||0)>=95});const opportunity=analyticsMoneyValue(records);const prevOpportunity=analyticsMoneyValue(prev);const sourceMap={};(S.sources||[]).forEach(function(s){sourceMap[s.name]=records.filter(function(r){return (r.sourceIds||[]).includes(s.id)}).length});const recentRecords=[...records].sort(function(a,b){return String(b.createdAt||'').localeCompare(String(a.createdAt||''))}).slice(0,8);const recentEvents=[...events].sort(function(a,b){return String(b.createdAt||'').localeCompare(String(a.createdAt||''))}).slice(0,8);return '<div class="analyticsShell"><section class="analyticsTop"><div><h2>Analytics Command Center</h2><p class="muted">Track business activity, record creation, priorities, sources, and next actions.</p></div>'+analyticsControls()+'</section><section class="analyticsKpis">'+analyticsKpi('New records',records.length,records.length,prev.length,analyticsRangeLabel())+analyticsKpi('New leads',leads.length,leads.length,prevLeads.length,'People and intakes')+analyticsKpi('Active deals',deals.length,deals.length,prev.filter(function(r){return r.type==='Deal'}).length,money(opportunity))+analyticsKpi('Open tasks',tasks.length,tasks.length,prev.filter(function(r){return r.type==='Task'}).length,'Follow-ups')+analyticsKpi('Highest priority',highest.length,highest.length,prev.filter(function(r){return Number(r.priorityScore||0)>=95}).length,'Score 95+')+analyticsKpi('Opportunity',money(opportunity),opportunity,prevOpportunity,'Deal value')+'</section><section class="analyticsMain"><div class="analyticsCard"><div class="in"><h3>Business activity over time</h3><p class="muted">Records and captured events by day.</p>'+analyticsChart(records,events)+'</div></div><div class="analyticsCard"><div class="in"><h3>Smart insights</h3>'+analyticsInsights(records,events)+'</div></div></section><section class="analyticsBreakdowns">'+analyticsRows('Record types',analyticsCounts(records,function(r){return r.type}))+analyticsRows('Priority distribution',analyticsCounts(records,analyticsPriorityName))+analyticsRows('Source performance',sourceMap)+'</section>'+analyticsFunnel(records)+'<section class="analyticsTables"><div class="analyticsCard"><div class="in"><h3>Recent records</h3>'+(recentRecords.length?recentRecords.map(function(r){return '<div class="analyticsMiniRecord"><b>'+esc(r.title)+'</b><p>'+esc(r.type)+' · '+Math.round(r.priorityScore||0)+' priority · '+esc((r.createdAt||'').slice(0,10))+'</p></div>'}).join(''):'<p class="muted">No recent records.</p>')+'</div></div><div class="analyticsCard"><div class="in"><h3>Recent activity events</h3>'+(recentEvents.length?recentEvents.map(function(e){return '<div class="analyticsMiniRecord"><b>'+esc(e.type)+'</b><p>'+esc(e.sourceUrl||e.siteId||'')+' · '+esc((e.createdAt||'').slice(0,16).replace('T',' '))+'</p></div>'}).join(''):'<p class="muted">No recent events.</p>')+'</div></div></section></div>'}`;

const analyticsBindCode = "let analyticsRange=document.getElementById('analyticsRange');if(analyticsRange)analyticsRange.onchange=function(){S.analyticsRange=analyticsRange.value;render()};let analyticsSource=document.getElementById('analyticsSource');if(analyticsSource)analyticsSource.onchange=function(){S.analyticsSource=analyticsSource.value;render()};let analyticsRefresh=document.getElementById('analyticsRefresh');if(analyticsRefresh)analyticsRefresh.onclick=async function(){analyticsRefresh.disabled=true;analyticsRefresh.textContent='Refreshing...';try{await load();render()}catch(err){analyticsRefresh.textContent='Refresh failed';setTimeout(function(){let b=document.getElementById('analyticsRefresh');if(b){b.disabled=false;b.textContent='Refresh'}},1800)}};let analyticsReport=document.getElementById('analyticsReport');if(analyticsReport)analyticsReport.onclick=async function(){const old=analyticsReport.textContent;analyticsReport.disabled=true;analyticsReport.textContent='Generating...';try{await api('/api/reports/generate',{method:'POST',body:JSON.stringify({})});await load();analyticsReport.textContent='Report created';setTimeout(function(){let b=document.getElementById('analyticsReport');if(b){b.disabled=false;b.textContent=old}},1800)}catch(err){analyticsReport.textContent='Could not generate';setTimeout(function(){let b=document.getElementById('analyticsReport');if(b){b.disabled=false;b.textContent=old}},2200)}};";

const generatedAnalyticsPatch =
  "source = source.replace(" + JSON.stringify("function render(){") + ", " + JSON.stringify(analyticsClientCode + "\nfunction render(){") + ");\n" +
  "source = source.replace(/if\\(S\\.tab==='analytics'\\)\\{h=[\\s\\S]*?\\}if\\(S\\.tab==='crm'\\)/, " + JSON.stringify("if(S.tab==='analytics')h=analyticsContent();if(S.tab==='crm')") + ");\n" +
  "source = source.replace(" + JSON.stringify("let typeSelect=document.getElementById('manualType');") + ", " + JSON.stringify(analyticsBindCode + "let typeSelect=document.getElementById('manualType');") + ");\n";

let responsive = await fs.readFile(responsiveSourcePath, "utf8");
const styleMarker = "`;\n\nconst crmContentNeedle";
if (!responsive.includes(styleMarker)) {
  throw new Error("Could not find responsiveCss closing marker in src/server-responsive.js");
}
responsive = responsive.replace(styleMarker, "\n" + wideCss + "\n" + analyticsCss + "\n`;\n\nconst crmContentNeedle");

const injectionNeedle = "const injection = wizardInjection + crmInjection + ";
if (!responsive.includes(injectionNeedle)) {
  throw new Error("Could not find responsive injection builder in src/server-responsive.js");
}
responsive = responsive.replace(injectionNeedle, "const analyticsInjection = " + JSON.stringify(generatedAnalyticsPatch) + ";\n" + injectionNeedle + "analyticsInjection + ");

await fs.writeFile(analyticsRuntimePath, responsive);
await import(`${pathToFileURL(analyticsRuntimePath).href}?v=${Date.now()}`);
