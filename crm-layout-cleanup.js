(function(){
  // Legacy layout controller disabled.
  // The distinct CRM workspace now owns sidebar tabs, top title, and AI top-bar tools.
  window.__constravaCrmSideNavOnlyLoaded = true;
  console.log('Legacy CRM layout cleanup skipped; distinct CRM workspace is active.');
})();
