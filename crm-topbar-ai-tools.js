(function(){
  // Disabled as a separate controller to prevent top-bar flicker.
  // AI top-bar tools are now owned by the distinct CRM workspace script.
  window.__constravaCrmTopbarAiToolsLoaded = true;
  console.log('Separate CRM topbar AI script skipped; distinct CRM workspace owns the top bar.');
})();
